# Pilha para estudo

Código apresentado pelo professor para estudo da estrutura de dados pilha.

O código-fonte original foi mantido sem modificações.

## Compilação e execução

Execute a partir de `01-pilha-estudo/c`:

```bash
gcc main.c -o pilha.bin
./pilha.bin
```